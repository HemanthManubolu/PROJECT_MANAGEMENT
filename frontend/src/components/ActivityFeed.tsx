import type { Activity } from '../types/activity';
import { dateTime, label } from '../utils/format';
export const ActivityFeed = ({ activities, title = 'Recent activity' }: { activities: Activity[]; title?: string }): React.JSX.Element => <section className="card"><div className="card-heading"><h2>{title}</h2><span>{activities.length} events</span></div><div className="divide-y divide-slate-100">{activities.length === 0 ? <p className="empty">No activity yet.</p> : activities.map((activity) => <div className="flex gap-3 py-4" key={activity.id}><div className="mt-1 size-2 shrink-0 rounded-full bg-blue-500"/><div className="min-w-0"><p className="text-sm text-slate-700"><strong>{activity.user.name}</strong> moved <strong>{activity.task.title}</strong>{activity.oldStatus && activity.newStatus ? <> from {label(activity.oldStatus)} to {label(activity.newStatus)}</> : ''}</p><p className="mt-1 text-xs text-slate-400">{activity.project.name} · {dateTime(activity.createdAt)}</p></div></div>)}</div></section>;

import { ClipboardList, FolderKanban, LayoutDashboard, Users } from 'lucide-react';
import { NavLink } from 'react-router-dom';
import { useAppSelector } from '../app/hooks';
const base = 'flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition';
export const Sidebar = (): React.JSX.Element => { const user = useAppSelector((state) => state.auth.user); const links = [{ to: '/dashboard', label: 'Dashboard', icon: LayoutDashboard }, { to: '/tasks', label: 'My tasks', icon: ClipboardList }, ...(user?.role === 'DEVELOPER' ? [] : [{ to: '/projects', label: 'Projects', icon: FolderKanban }]), ...(user?.role === 'ADMIN' ? [{ to: '/team', label: 'Team', icon: Users }] : [])]; return <aside className="sidebar"><div className="mb-8 px-3"><div className="mb-2 flex size-9 items-center justify-center rounded-xl bg-gradient-to-br from-blue-500 to-indigo-600 font-bold text-white">A</div><h1 className="font-semibold text-slate-800">Agency OS</h1><p className="text-xs text-slate-400">Client delivery</p></div><nav className="space-y-1">{links.map(({ to, label, icon: Icon }) => <NavLink end={to === '/dashboard'} key={to} to={to} className={({ isActive }) => `${base} ${isActive ? 'bg-blue-50 text-blue-700' : 'text-slate-600 hover:bg-slate-50'}`}><Icon size={17}/>{label}</NavLink>)}</nav><div className="mt-auto rounded-xl bg-slate-50 p-3 text-xs text-slate-500"><p className="font-medium text-slate-700">{user?.role.replace('_', ' ')}</p><p className="mt-1">Access is enforced by the API.</p></div></aside>; };
